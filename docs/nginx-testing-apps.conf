# ============================================================
# TESTING APPS - Add these locations to /etc/nginx/conf.d/portfolio.conf
# Before the closing } of the main server block
# ============================================================

    # darkflow-mind-mapper (port 3010)
    location /darkflow {
        return 301 /darkflow/;
    }
    location /darkflow/ {
        proxy_pass http://127.0.0.1:3010/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }

    # gmat-mastery-suite (port 3012)
    location /gmat {
        return 301 /gmat/;
    }
    location /gmat/ {
        proxy_pass http://127.0.0.1:3012/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }

    # losk (port 3013)
    location /losk {
        return 301 /losk/;
    }
    location /losk/ {
        proxy_pass http://127.0.0.1:3013/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }

    # got-hired-ai (port 3014)
    location /got-hired {
        return 301 /got-hired/;
    }
    location /got-hired/ {
        proxy_pass http://127.0.0.1:3014/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }

# ============================================================
# NOTE: bh-ai-79, zen-tot have existing Docker configs
# purple-lotus is Expo and needs special handling
# ============================================================
